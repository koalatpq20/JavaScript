// JavaScript source code
//export const capitalizeString = str => { return str.toUpperCase(); }

const capitalizeString = (str) => { return str.toUpperCase(); }

let Employee = {
    "name": "Jane",
    "age": 25,
    "address": "123 street"
};

export { capitalizeString, Employee}